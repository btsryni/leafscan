import 'package:flutter/material.dart';

/// LeafScan Design System — Fresh, high-fidelity light organic green color tokens.
class AppColors {
  AppColors._();

  // ── Primary Greens ──
  static const Color primary = Color(0xFF2A9D57);       // Brand leaf green
  static const Color leafGreen = Color(0xFF2E7D32);     // Medium dark green
  static const Color darkGreen = Color(0xFF0F3E1E);     // Deep forest green for navigation/heavy components
  static const Color lightGreen = Color(0xFFE8F5E9);    // Extremely soft background green highlight
  static const Color accentGreen = Color(0xFF22C55E);   // Vibrant emerald green
  static const Color mintGreen = Color(0xFFDCFCE7);     // Soft mint background badge

  // ── Neutrals ──
  static const Color background = Color(0xFFF9FAFB);    // Ultra fresh slate white
  static const Color cardSurface = Color(0xFFFFFFFF);   // Pure white
  static const Color border = Color(0xFFE2E8F0);        // Slate grey border line
  static const Color textPrimary = Color(0xFF1E293B);   // Deep slate grey text
  static const Color textSecondary = Color(0xFF64748B); // Medium slate text
  static const Color textHint = Color(0xFF94A3B8);      // Soft hint text
  static const Color divider = Color(0xFFF1F5F9);       // Light divider

  // ── Semantic ──
  static const Color healthyBadge = Color(0xFF10B981);  // Green success
  static const Color warningBadge = Color(0xFFF59E0B);  // Amber warning
  static const Color virusBadge = Color(0xFFEF4444);    // Red critical
  static const Color white = Color(0xFFFFFFFF);
  static const Color redAccent = Color(0xFFDC2626);     // Darker red for delete buttons
}
